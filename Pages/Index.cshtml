@page
@model IndexModel
@{
    ViewData["Title"] = "İnteraktif Şube Girişi";
}

<div class="login_container">
    <div class="login_card">
        <div class="login_header">
            <div class="logo_container">
                <svg class="logo_icon" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M12 2L2 7L12 12L22 7L12 2Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    <path d="M2 17L12 22L22 17" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    <path d="M2 12L12 17L22 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
            </div>
            <h1 class="login_title">İnteraktif Şube</h1>
            <p class="login_subtitle">Hoş geldiniz! Devam etmek için giriş yapın</p>
        </div>

        <form id="login_form" class="login_form" method="post">
            <div class="login_type_selector">
                <label class="type_option">
                    <input type="radio" name="login_type" value="gsm" checked />
                    <span class="type_label">GSM ile Giriş</span>
                </label>
                <label class="type_option">
                    <input type="radio" name="login_type" value="tckn" />
                    <span class="type_label">TC Kimlik ile Giriş</span>
                </label>
            </div>

            <div class="input_group">
                <label class="input_label" id="identifier_label">GSM Numarası</label>
                <div class="input_wrapper">
                    <input type="text" 
                           id="identifier" 
                           name="identifier" 
                           class="form_input" 
                           placeholder="05XX XXX XX XX"
                           maxlength="11"
                           autocomplete="off"
                           required />
                    <span class="input_icon">
                        <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <path d="M16 7C16 9.20914 14.2091 11 12 11C9.79086 11 8 9.20914 8 7C8 4.79086 9.79086 3 12 3C14.2091 3 16 4.79086 16 7Z" stroke="currentColor" stroke-width="2"/>
                            <path d="M12 14C8.13401 14 5 17.134 5 21H19C19 17.134 15.866 14 12 14Z" stroke="currentColor" stroke-width="2"/>
                        </svg>
                    </span>
                </div>
                <span class="input_error" id="identifier_error"></span>
            </div>

            @if (!Model.HasKvkkConsent)
            {
                <div class="kvkk_container">
                    <label class="kvkk_checkbox">
                        <input type="checkbox" id="kvkk_accept" name="kvkkAccept" required />
                        <span class="checkmark"></span>
                        <span class="kvkk_text">
                            <a href="#" class="kvkk_link">KVKK Aydınlatma Metni</a>'ni okudum, anladım ve kabul ediyorum.
                        </span>
                    </label>
                </div>
            }

            <button type="submit" class="btn_primary" id="login_btn">
                <span class="btn_text">Giriş Yap</span>
                <span class="btn_loader hidden">
                    <svg class="spinner" viewBox="0 0 24 24">
                        <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4" fill="none" opacity="0.25"/>
                        <path fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"/>
                    </svg>
                </span>
            </button>
        </form>

        <div class="login_footer">
            <p>Hesabınız yok mu? <a href="/SignUp" class="link_primary">Üye Ol</a></p>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            var $identifier_input = $('#identifier');
            var $identifier_label = $('#identifier_label');
            var $login_form = $('#login_form');
            var $login_btn = $('#login_btn');
            var login_type = 'gsm';

            console.log('Login form initialized');

            // Handle login type change
            $('input[name="login_type"]').on('change', function() {
                login_type = $(this).val();
                $identifier_input.val('');
                clear_error('identifier');
                console.log('Login type changed to:', login_type);

                if (login_type === 'gsm') {
                    $identifier_label.text('GSM Numarası');
                    $identifier_input.attr('placeholder', '05XX XXX XX XX');
                    $identifier_input.attr('maxlength', '11');
                } else {
                    $identifier_label.text('TC Kimlik No');
                    $identifier_input.attr('placeholder', '11 haneli TC Kimlik No');
                    $identifier_input.attr('maxlength', '11');
                }
            });

            // Input formatting
            $identifier_input.on('input', function() {
                let value = $(this).val().replace(/\D/g, '');
                if (value.length > 11) {
                    value = value.substring(0, 11);
                }
                $(this).val(value);
            });

            // Form submission
            $login_form.on('submit', function(e) {
                console.log('Form submit triggered');
                
                const identifier = $identifier_input.val();
                const kvkk_checkbox = $('#kvkk_accept');
                const kvkk_accepted = kvkk_checkbox.length === 0 || kvkk_checkbox.is(':checked');

                console.log('Identifier:', identifier);
                console.log('Login type:', login_type);
                console.log('KVKK accepted:', kvkk_accepted);

                // Validate identifier
                if (!identifier || identifier.length !== 11) {
                    e.preventDefault();
                    show_error('identifier', 'Lütfen 11 haneli numara giriniz');
                    console.log('Validation failed: length');
                    return false;
                }

                if (login_type === 'gsm' && !identifier.startsWith('05')) {
                    e.preventDefault();
                    show_error('identifier', 'GSM numarası 05 ile başlamalıdır');
                    console.log('Validation failed: GSM format');
                    return false;
                }

                if (!kvkk_accepted) {
                    e.preventDefault();
                    show_error('identifier', 'KVKK metnini kabul etmelisiniz');
                    console.log('Validation failed: KVKK');
                    return false;
                }

                // Show loading state
                console.log('Showing loading state');
                $login_btn.addClass('loading');
                $login_btn.find('.btn_text').hide();
                $login_btn.find('.btn_loader').removeClass('hidden').show();
                $login_btn.prop('disabled', true);

                // Allow form to submit normally
                console.log('Form submitting to server...');
                return true;
            });

            function show_error(field_id, message) {
                $('#' + field_id + '_error').text(message).addClass('show');
                $('#' + field_id).addClass('error');
            }

            function clear_error(field_id) {
                $('#' + field_id + '_error').text('').removeClass('show');
                $('#' + field_id).removeClass('error');
            }
        });
    </script>
}
