@page
@model DashboardModel
@{
    ViewData["Title"] = "Dashboard";
}

<div class="dashboard_container">
    <div class="dashboard_card">
        <h1>Hoş Geldiniz!</h1>
        <p>Giriş başarılı. Dashboard sayfasına yönlendirildiniz.</p>
        <form method="post" asp-page-handler="Logout">
            <button type="submit" class="btn_primary">Çıkış Yap</button>
        </form>
    </div>
</div>
